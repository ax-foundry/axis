# Derived from pyproject.toml — used for Docker layer caching.
# For local development, prefer: pip install -e ".[dev,graph]"

# FastAPI and ASGI
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.17

# Data processing
pandas>=2.2.3
numpy>=2.0.0
scikit-learn>=1.5.0

# Validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Configuration
python-dotenv>=1.0.1

# AI/ML
openai>=1.55.0
anthropic>=0.49.0
pydantic-ai==1.47.0
pydantic-graph==1.47.0
sse-starlette>=2.0.0
openai-agents>=0.0.14

# Database connectivity
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0
duckdb>=1.5.0
pytz>=2024.1

# Graph database ([graph] optional extra in pyproject.toml)
falkordb>=1.0.0

# Utilities
orjson>=3.10.0
httpx>=0.28.0

# Dependency pins — from local pip freeze to prevent pip resolution-too-deep.
# These are the versions proven compatible with axion v0.2.2 + pydantic-ai + AXIS deps.
opentelemetry-api==1.39.1
opentelemetry-sdk==1.39.1
opentelemetry-exporter-otlp-proto-grpc==1.39.1
opentelemetry-exporter-otlp-proto-http==1.39.1
opentelemetry-exporter-otlp-proto-common==1.39.1
opentelemetry-proto==1.39.1
grpcio==1.76.0
langfuse==4.0.1
litellm==1.80.13
logfire==4.17.0
deepeval==3.1.0
click==8.1.8

# Axion (evaluation framework)
axion[langfuse] @ git+https://github.com/ax-foundry/axion.git@v0.2.2
