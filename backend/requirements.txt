# Derived from pyproject.toml — used for Docker layer caching.
# For local development, prefer: pip install -e ".[dev,graph]"

# FastAPI and ASGI
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.17

# Data processing
pandas>=2.2.3
numpy>=2.0.0
scikit-learn>=1.5.0

# Validation
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Configuration
python-dotenv>=1.0.1

# AI/ML
openai>=1.55.0
anthropic>=0.49.0
pydantic-ai>=0.0.14
pydantic-graph>=0.0.10
sse-starlette>=2.0.0
openai-agents>=0.0.14

# Database connectivity
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0
duckdb>=1.5.0
pytz>=2024.1

# Graph database ([graph] optional extra in pyproject.toml)
falkordb>=1.0.0

# Utilities
orjson>=3.10.0
httpx>=0.28.0

# OpenTelemetry + gRPC pins — prevent pip resolution-too-deep caused by
# deepeval (otel-grpc) + langfuse (otel-http) version matrix explosion.
grpcio>=1.67.0,<2.0.0
opentelemetry-api>=1.28.0,<2.0.0
opentelemetry-sdk>=1.28.0,<2.0.0
opentelemetry-exporter-otlp-proto-grpc>=1.28.0,<2.0.0
opentelemetry-exporter-otlp-proto-http>=1.28.0,<2.0.0

# Axion (evaluation framework)
axion[langfuse] @ git+https://github.com/ax-foundry/axion.git
