# AXIS Frontend Environment Variables

# API URL
NEXT_PUBLIC_API_URL=http://localhost:8500

# Database connection defaults are configured on the BACKEND (not here).
# See backend/.env.example for DB_URL, DB_HOST, DB_TABLE, etc.
